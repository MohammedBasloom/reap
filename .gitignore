# Secrets — never commit tokens
.mcp.json
.netlify-token
netlify-token.txt
.netlify-token.txt
.github-token
github-token.txt

# Local build/test artifacts
build/

# Machine-specific Claude Code state (permission grants for this device).
# .claude/launch.json IS tracked — the dev-server config should travel.
.claude/*
!.claude/launch.json
